#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

#include <iostream>
#include <string>
using namespace std;

void Error(string);
void FatalError(string);

void ArrayOutput(char*);
int isZeroChar(char);
int isLineChar(char);
int isEofChar(char);
